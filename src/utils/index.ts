import * as getCartProduct from './getCartProducts';
import * as removeImageBackground from './removeImageBackground'
import * as jwtUtils from './jwt'

export const Utils = {
  getCartProduct,
  removeImageBackground,
  jwtUtils
}